server:
  port: 10000
spring:
  application:
    name: app-admin
  boot:
    admin:
      turbine:
        enabled: true
#        clusters: default
#        location: hystrix
#management:
 # security:
  #  enabled: false #关闭已有安全认证
#security:
 # basic:
  #  enabled: true #关闭默认认证，使用自定义认证处理
  #user:
   # name: admin
    #password: admin123
hystrix.stream.dashboard.intervalInMilliseconds: 5000


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true