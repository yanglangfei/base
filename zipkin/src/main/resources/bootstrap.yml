server:
  port: 9411
spring:
  application:
    name: zipkin
  cloud:
    config:
      fail-fast: true
      uri: http://config:8888
      profile: dev
management:
  security:
    enabled: true
security:
  basic:
    enabled: false
  user:
    name: admin
    password: admin123
hystrix.stream.dashboard.intervalInMilliseconds: 5000